spring:
  datasource:
    url: jdbc:h2:mem:test_db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

eureka:
  client:
    enabled: false

# 테스트용 JWT 설정
jwt:
  secret: test-secret-key-for-company-service
  expiration: 86400000