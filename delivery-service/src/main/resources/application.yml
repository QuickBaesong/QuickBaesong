server:
  port: 8085

spring:
  application:
    name: delivery-service
    profiles:
      active: local
      config:
        import: optional:file:./.env

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/